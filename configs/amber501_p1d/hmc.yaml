# Path to the training run directory (contains config.yaml and checkpoints/)
run_dir: outputs/amber501_p1d/2026-03-16_10-19-30

output_dir: null

data:
  dataset_file: /Users/diegogonzalez/Documents/Research/ENIGMA/BNRE-HMC/jax_bnre_hmc/datasets/amber501/mocks_z5.00_snr30.0.hdf5  # set via CLI or override; HDF5 with params, subset_of_mock_datasets

num_chains: 4
num_warmup: 8000
num_samples: 8000

n_observations: 500
n_plots: 100
seed: 2401

# Prior built from convex hull of scaled params in dataset (same dataset_file)
prior:
  type: convex_hull

corner_labels: ["z_mid", "Delta_z", "A_z", "T_0", "<F>"]

# Data loading splits (must match script logic)
data_split_test_fraction: 0.2
data_split_validation_fraction: 0.2
