# Path to the training run directory (contains config.yaml and checkpoints/)
run_dir: outputs/sinusoid_transformer/2026-03-16_21-09-33

output_dir: null

data:
  dataset_file: /Users/diegogonzalez/Documents/Research/ENIGMA/BNRE-HMC/jax_bnre_hmc/datasets/sinusoid/sinusoid_noisy_with_masks.h5  # set via CLI or override; HDF5 with theta, y_obs, mask

num_chains: 4
num_warmup: 4000
num_samples: 4000

n_observations: 500
n_plots: 25
seed: 2401

# Prior in scaled space [-1,1]^4
prior:
  type: box
  low: [-1.0, -1.0, -1.0, -1.0]
  high: [1.0, 1.0, 1.0, 1.0]

corner_labels: ["A", "f", "phi", "b"]
